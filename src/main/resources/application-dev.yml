spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:campus_express;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data.sql

wechat:
  access-token-url: https://api.weixin.qq.com/cgi-bin/token
  subscribe-send-url: https://api.weixin.qq.com/cgi-bin/message/subscribe/send?access_token=%s
  debug: true

oss:
  enabled: false
  endpoint:
  access-key-id:
  access-key-secret:
  bucket-name:
  folder: evidence
  url-prefix:
  local-dir: uploads/evidence

warning:
  webhook-url:
  scan-cron: 0 0 2 * * ?
  wechat:
    enabled: false
    template-id:
    page: pages/package/list/index
    miniprogram-state: formal
    lang: zh_CN
    title-field: thing1
    station-field: thing2
    code-field: character_string3
    date-field: time4
    remark-field: thing5
